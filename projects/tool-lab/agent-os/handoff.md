# Handoff

- Continue from `docs/local-operating-summary.md` first.
- Primary entrypoint: `python3 -m agent_os.cli`.
- Check `status.md`, `tasks.md`, and latest file in `runs/`.
- Verify with `python3 -m unittest discover -s tests -v`.
