# Knowledge

- The repo already uses markdown-heavy operational state, so this project follows the same pattern.
- Python 3.13 is available.
- `pytest` is not installed, so the test harness uses `unittest`.
- Network access is not required for milestone 1.
