# 2026-04-28

## What Happened

- Confirmed local working area for this system is `/Users/banirisset/2_Areas/banirisset`.
- Diagnosed Hermes Telegram runtime on VPS and verified live service is `hermes-gateway.service`.
- Verified Hermes live config source of truth is `/home/hermes/.hermes/.env` and `/home/hermes/.hermes/config.yaml`, not legacy OpenClaw config.
- Verified active Telegram bot token in Hermes points to bot id `8648903806` (`@survivorset_bot`).
- Restored old REED group mapping so legacy `SecondBrain OS` topic `INBOX` could register again in Hermes channel directory.
- Confirmed DM works and legacy `INBOX` topic reappeared in Hermes channel directory.

## What Was Decided

- `INBOX` is not killed.
- `INBOX` is an intake layer only, not a final knowledge bucket.
- Operational destinations remain `tasks`, `content`, `personal-crm`, `ops`, `knowledge-base`, and `updates`.
- `Hermes` is the working/operational memory layer.
- `Wiki` is the canonical knowledge layer.
- `Hermes` must not become a wiki.
- `Wiki` must not become agent RAM.
- Mixed messages may be split during routing.
- Example routing rule:
  - idea/content asset -> `content`
  - time-based reminder/action -> `tasks`
- Canon candidate buckets were narrowed conceptually to:
  - `Research`
  - `Frameworks`
  - `SOPs`
  - `Decisions`
  - `Incidents`
- `task` and `daily recap` should not be canon wiki categories.

## What Remains Open

- Finalize official canon bucket set for wiki layer.
- Decide whether `Sources` should be a formal canon bucket or only a supporting layer inside other buckets.
- Decide final short/hard wording for AGENTS/wiki boundary policy if it should be promoted into canon docs later.

## Handoff Facts

- Bot utama: `@survivorset_bot`
- Hermes Telegram service: `hermes-gateway.service`
- Legacy `INBOX` topic is now live again in Hermes channel directory.
- Fresh agent handoff should start from:
  - `AGENTS.md`
  - `SOUL.md`
  - `USER.md`
  - `openclaw.md`
  - `daily.md`
  - `memory/2026-04-28.md`
