# 2026-04-30

## What Happened

- Verifikasi code fixes hermes-agent (setMyCommands launcher-only, /menu inline keyboard, AIAgent constructor compat filter)
- Push ke GitHub gagal 403 karena user (`ariwarli`) tidak punya push access ke `NousResearch/hermes-agent`
- Mem-fork repo ke `ariwarli/hermes-agent` via `gh repo fork`
- Merge upstream `NousResearch/main` (update besar: 387 file, 33900+ baris, 100+ commits) ke fork lokal
- Sync VPS ke fork + restart `hermes-cli.main gateway run --replace`
- **SAVED SESSION** ditengah proses karena user bilang "save session ini reed"
- **Telegram menu masih belum fixed** — server-side fix deployed (`delete_my_commands` + `set_my_commands` hanya `/menu`), tapi client cache masih persists. User memilih **skip** clear cache client — next step modifikasi code pakai `scope=BotCommandScopeAllPrivateChats()`
- **Bot sudah responsif** — screenshot menunjukkan reply panjang tentang auto-reminder & task tracking setelah model default diganti ke `deepseek-v4-flash`
- **python-pptx installed & configured** — Hermes kini bisa generate dan auto-deliver PPTX ke Telegram via `MEDIA:` tag tanpa manual user steps
- **SSH hardening** — SSH locked down ke Tailscale only (`100.113.246.119:22`), user non-root `banirisset` dibuat, public SSH (`167.253.158.103:22`) refused

## Code Fixes Verified

1. **setMyCommands launcher-only** — `_TELEGRAM_CURATED_MENU_ORDER` hanya `("menu",)`, register satu command `/menu`
2. **Inline launcher `/menu`** — sections: Session, Model & Config, Help dengan sub-actions
3. **AIAgent compat filter** — `_filter_agent_init_kwargs` inspect signature runtime, skip kwargs yang tidak didukung

## Telegram Menu Live Issue (Unsolved)

- **Tombol slash masih menampilkan semua commands** — ini karena Telegram client cache command list lama. Code fix memang sudah benar (hanya `/menu` di-register), tapi client perlu `delete_my_commands` sebelum `set_my_commands` supaya cache di-clear
- **Fix diterapkan**: menambahkan `await self._bot.delete_my_commands()` sebelum `set_my_commands()` di `gateway/platforms/telegram.py`
- **User skip clear cache**: User memilih tidak clear cache client. Next step: modifikasi code pakai `scope` parameter agar lebih reliable

## /menu “Menu closed” Behavior

- Ini **bukan bug** — itu expected behavior. User membuka launcher, menekan “Close” → launcher message ter-edit jadi “Menu closed”. Keyboard launcher hilang.
- Jika user ingin membuka kembali, ketik `/menu` lagi. Saran: **tidak menekan tombol “Close”** kalau tidak perlu — langsung pilih command dari launcher.

## Bot “Bolot” (Lambat) Issue — FIXED

- **Root cause**: `model.default = qwen3-coder:480b` — model 480B parameter, sangat lambat dan berat
- **Fix config**: ubah `model.default` ke `deepseek-v4-flash` di `/home/hermes/.hermes/config.yaml`
- **Hasil**: Bot sudah responsif — user mendapat reply panjang tentang auto-reminder & task tracking
- **Fallback tetap sama** (ollama-cloud multi-model) tapi providernya single (ollama-cloud). Idealnya fallback juga memakai provider berbeda, tapi saat ini config masih ollama-only.

## PPTX Auto-Generation — FIXED

- **Masalah**: Hermes memberikan instruksi manual ke user (install npm, save script, run sendiri) saat diminta generate PPTX
- **Root cause**: Skill default pakai `pptxgenjs` (Node.js) yang tidak tersedia di environment. Tidak ada fallback guide ke method alternatif
- **Fix**: Install `python-pptx` di VPS + buat `python-pptx.md` guide + update `SKILL.md` Quick Reference dan Dependencies
- **File baru**: `skills/productivity/powerpoint/python-pptx.md` — tutorial python-pptx native, host-visible path, MEDIA: tag auto-delivery
- **Commit**: `0668437f` pushed ke `ariwarli/hermes-agent`, synced ke VPS

## SSH Hardening — COMPLETED

- **User non-root dibuat**: `banirisset` dengan SSH key dari root, sudo tanpa password
- **SSH config**: `ListenAddress 100.113.246.119` (Tailscale IP only), `PasswordAuthentication no`, `AllowUsers root banirisset`
- **Public SSH refused**: `167.253.158.103:22` → `Connection refused`
- **Tailscale SSH verified**: `root@100.113.246.119` ✅, `banirisset@100.113.246.119` ✅, `sudo whoami` → root ✅

## Action Items (Sebelum Skip)

1. ~~Restart Telegram client untuk clear command cache~~ → **USER SKIP**
2. ~~Test `/menu` → pilih command tanpa menekan “Close”~~ → **USER SKIP**
3. ✅ Test bot responsiveness — model lebih cepat harusnya lebih responsif → **CONFIRMED WORKING**

## Next Steps (Unresolved / Todo)

- **Telegram menu**: Modifikasi code `set_my_commands` pakai `scope=BotCommandScopeAllPrivateChats()` — ini akan memastikan menu hanya `/menu` di semua chat
- **GitHub sync**: Push berhasil ke fork, tapi upstream NousResearch masih belum update (butuh PR kalau ingin merge)
- **VPS service restart**: `systemctl --user` gagal (DBUS tidak ter-set). Workaround: `nohup` manual, tapi idealnya perlu fix systemd user session atau pakai systemd `--machine=hermes@.host --user`
- **Model selection otomatis**: Belum ada per-topic/channel model routing di gateway — sekarang hanya global default + per-session `/model` command
- **Tailscale SSH built-in**: Tailscale CLI available, bisa explore `tailscale ssh` feature kalau mau lebih otonom (gak perlu manual authorized_keys)