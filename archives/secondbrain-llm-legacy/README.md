# SECONDBRAIN-LLM Legacy Archive

Archived content from `ariwarli/SECONDBRAIN-LLM` repo, consolidated into SecondBrain-OS on 2026-05-11.

## Why archived
- OpenClaw runtime retired; Hermes (`/home/hermes/.hermes/`) is the active system
- SecondBrain-OS is now the single workspace repo for both Mac and VPS

## Structure
- `root-docs/` — standalone docs from SECONDBRAIN-LLM root (blueprints, guides, notes)
- `docs/` — unique docs not already in SecondBrain-OS/docs
- `openclaw/` — OpenClaw scripts, config, and operational docs
- `prompts/` — prompt templates
- `scheduler/` — scheduler configs

## What was NOT archived (already existed in SecondBrain-OS)
- `brand-os/` → already at `brand-os/`
- `clients/` → already at `clients/`
- `automation/` → already at `automation/`
- `archives/` → already at `archives/`
- `core/` identity files (SOUL.md, USER.md, etc.) → already at `core/`
- `knowledge-base/` → already at `knowledge-base/`
- `inbox/` → already at `inbox/`
