Lanjutkan perbaikan `save-update-sbllm` di project `SECONDBRAIN-LLM`, fokus
  khusus pada kualitas bagian `done` agar handoff untuk agen kosong lebih padat
  dan presisi.

  Konteks penting:
  - File utama: `/Users/banirisset/banirisset/SECONDBRAIN-LLM/knowledge-base/
  tools/session_handoff.py`
  - Saat ini `evidence` sudah dibuat wajib untuk write-mode.
  - Masalah yang tersisa: fallback `done` masih terlalu banyak menarik konteks
  lama dari active session / checkpoint lama, sehingga `Current State` dan `What
  Changed` di handoff/checkpoint bisa berisi item lama yang bukan perubahan sesi
  terakhir.
  - Targetnya bukan menghapus fallback sepenuhnya, tetapi membuat `done` hanya
  berisi perubahan yang benar-benar relevan untuk save-update saat ini.

  Tujuan:
  1. Rapikan logika sintesis `done` agar tidak “menarik ekor” konteks lama.
  2. Pastikan handoff untuk agen kosong menampilkan status terbaru secara
  ringkas, bukan campuran history lama.
  3. Pertahankan perilaku fallback untuk `blockers` dan `next` bila masih masuk
  akal.
  4. Jangan longgarkan aturan bahwa `evidence` wajib eksplisit untuk write-mode.

  Yang perlu kamu kerjakan:
  - Audit logika `synthesize_context_fallbacks` dan sumber fallback `done`.
  - Ubah strategi fallback `done` supaya lebih ketat. Prioritas:
    - pakai `--done` eksplisit jika ada
    - jika tidak ada, ambil hanya ringkasan yang paling baru dan paling relevan
    - hindari menyapu banyak item dari `## Current Decisions` / checkpoint lama
    - jangan masukkan placeholder atau item historis yang tidak
  merepresentasikan pekerjaan sesi ini
  - Pastikan output berikut ikut membaik:
    - checkpoint `## What Changed`
    - handoff `## Current State`
    - active session `## Latest Save-Update`
  - Jaga kompatibilitas perilaku existing selain bagian ini.

  Kriteria hasil:
  - Jika user memberi `--done`, output memakai itu sebagai sumber utama.
  - Jika `--done` tidak diberi, fallback maksimal singkat, relevan, dan tidak
  mengulang terlalu banyak context lama.
  - Handoff agen kosong terasa lebih padat dan lebih bisa langsung
  ditindaklanjuti.
  - `wiki_lint.py` tetap `OK`.
  - Tidak ada perubahan ke `AGENTS.md`.

  Verifikasi yang harus dijalankan:
  - dry-run tanpa `--done` tapi dengan `--evidence`
  - write-mode dengan `--done` eksplisit
  - write-mode tanpa `--done` untuk menguji fallback baru
  - `python3 -m py_compile tools/session_handoff.py tools/wiki_lint.py`
  - `python3 tools/wiki_lint.py --kb-root /Users/banirisset/banirisset/
  SECONDBRAIN-LLM/knowledge-base`

  Output yang saya butuhkan darimu:
  - ringkasan perubahan
  - alasan desain fallback `done` yang baru
  - hasil verifikasi
  - file yang diubah

  Versi lebih pendek jika mau langsung kirim:

  Rapikan `save-update-sbllm` di `SECONDBRAIN-LLM` agar fallback `done` tidak
  terlalu banyak menarik konteks lama dari active session/checkpoint lama. Fokus
  di `knowledge-base/tools/session_handoff.py`. `evidence` wajib write-mode
  harus tetap dipertahankan. Target: `What Changed` dan `Current State` di
  handoff jadi lebih padat, relevan, dan merepresentasikan sesi terakhir saja.
  Jalankan verifikasi dry-run, write-mode, `py_compile`, dan `wiki_lint`, lalu
  laporkan perubahan, alasan desain, hasil verifikasi, dan file yang diubah.